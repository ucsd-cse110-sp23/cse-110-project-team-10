The purpose of the List class is to manage and display a tasks remove tasks save tasks, and update the amount of tasks 

The purpose of the AppFrame class is to create the GUI for the app

The purpose of the addListeners method in the AppFrame class is to add listeners that connect the logic to the gui

It creates an instance of the AppFrame class in memory, which initializes and displays the To-Do List application.

========================================================================================================================================

Try running both ThreadingWithThread and ThreadingNoThread and try clicking on the buttons. 
Do you see any differences between the two programs? If you do, why do you think this happens?

The one with the thread is more responsive, since the calculation is not being caught 
up with the single thread it is still able to operate